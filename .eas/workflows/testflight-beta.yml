name: Controlled TestFlight Beta

on:
  workflow_dispatch:

jobs:
  build_ios:
    type: build
    params:
      platform: ios
      profile: production

  submit_ios:
    type: submit
    needs: [build_ios]
    params:
      platform: ios
      profile: production
